package org.example;

public record CipherSettings(int radix, int minLen, int maxLen) {

//    @Override
//    public String toString() {
//        return "radix=" + radix + ", length=[" + minLen + ", " + maxLen + "]";
//    }
}
